# ClusterIP: only accessible inside the cluster.
# The WebApp pod reaches the API via http://api-service:8000
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: wholesale-mlops
spec:
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
